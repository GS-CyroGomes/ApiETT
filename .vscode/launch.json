{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug (Docker Xdebug 2.5.5)",
            "type": "php",
            "request": "launch",
            "port": 9000,
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}/php56/src"
            }
        }
    ]
}
